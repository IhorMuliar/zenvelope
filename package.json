{
  "name": "zenvelope",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:core": "./scripts/build-core.sh",
    "test:core": "cargo test -p zenvelope-core && node scripts/smoke-core.mjs"
  }
}
