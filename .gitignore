# private working notes, never commit
HANDOFF.md
OUTREACH.md

# the first real envelope: the link fragment in here IS the money
M1-FUND.md
M1-FUND-qr.png

# the M3 sweep destination: a mnemonic and a link secret, both spendable
M3-DEST.md

node_modules/
dist/
.env*
*.params
.DS_Store
.claude/
# Rust and wasm build output, all of it regenerated by scripts/build-core.sh
target/
pkg/
web/src/wasm/

# Playwright
web/test-results/
web/playwright-report/

# M3 proving spike (throwaway): built wasm and its playwright deps
spike/pkg-*/
spike/package-lock.json
spike/test-results/
target-opt/
test-results/

# M4 threaded proving spike (throwaway): its own cargo target dir
target-mt/
# M4: the threaded core build has different rustflags, so it gets its own target dir
# (scripts/build-core.sh) rather than invalidating the stable one on every alternation
target-mt-core/
package-lock.json

# 2026-09-24 mainnet test envelopes: link secrets, spendable once funded
TEST-ENVELOPES-2026-09-24.md
TEST-ENVELOPES-QR.txt
TEST-ENVELOPES-*.png
