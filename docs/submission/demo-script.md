# Demo video script — 3 minutes

One run against `https://zenvelope.netlify.app`, COOP/COEP on, footer `proving: 4 threads`. Only waiting is
sped up, clock visible. The Solana leg is a **dry quote**; narration reads the captions.

| Time | On screen |
| --- | --- |
| 0:00–0:08 | Landing page: `0.001` into **Amount per envelope**, **Number of envelopes** left at `1`, `Happy birthday` into the message field. |
| 0:08–0:20 | **Create envelope**. The link appears; scroll to the ZIP-321 QR and the breakdown — envelope 0.001, fee 0.0003, pay 0.0013 ZEC. |
| 0:20–0:32 | **Phone shot.** Zodl, shielded balance visible: tap scan, point at the QR, the amount pre-fills, swipe to send, hold on it. |
| 0:32–0:45 | Laptop: the link pasted into a fresh browser profile. `/e` reads **You have an envelope**; the scan bar runs from the birthday to the tip, 3x. |
| 0:45–0:58 | The reveal: **0.0013 ZEC**, Ironwood, block 3,490,472, the memo. Footer `proving: 4 threads`, and "Keys ready". |
| 0:58–1:10 | **Where should it go?** → first card. Paste a `u1…` address. Review: 0.0013 in, 0.0001 network fee, 0.0003 service fee, 0.0009 out. |
| 1:10–1:40 | **Send it on**. The checklist paints `witness` (~9 s), `keys`, `proving` (~15 s, four threads), `broadcast`; 4x, clock unbroken. |
| 1:40–2:05 | **Sent**, with the txid; the explorer link clicked — `mainnet.zcashexplorer.app/transactions/ba0f91cfe7ca33dd269b692bf80027df2681a321215e90ab28c2a56260fa2aad`, confirmed, shielded outputs. |
| 2:05–2:18 | A second envelope → the Solana card. Trust boundary: four points, button disabled. Tick **I understand this leaves the shielded pool**. |
| 2:18–2:35 | **USDC** → **Generate a keypair here**; the base58 secret shows once, tick "I saved this key". The dry quote: what arrives, both dollar figures, the spread, "about 8 minutes", "Includes a 0.25% service fee to the swap provider". **Do not** click Get deposit address. |
| 2:35–3:00 | New tab: `0.001`, **Number of envelopes** `25`, **Create envelopes**; the table of 25 links, then **Download CSV** opened on `index,link,address,envelope_zec,send_zec,memo,payment_uri`. End card: `https://zenvelope.netlify.app`. |

Say **open**, **receive**, **send it on**: the drainer-lure word starting "cl-" is banned
on every screen and in every take. A re-take after broadcast needs a second funded
envelope.

## Captions

| # | In | Caption |
| --- | --- | --- |
| 1 | 0:00 | **An envelope is a link.** An amount, a private message, a ZIP-321 QR any wallet pays. |
| 2 | 0:20 | **Paid from Zodl on mainnet.** One output: 0.001 + 0.0003 flat fee. |
| 3 | 0:32 | **The recipient installs nothing.** The browser scans Zcash itself. |
| 4 | 0:45 | **0.0013 ZEC revealed.** Never in the link. Ironwood note, block 3,490,472. |
| 5 | 0:58 | **Where it goes is theirs.** An address pasted, or a wallet made here. 0.0009 received. |
| 6 | 1:10 | **Proved in this browser.** 13.5 s on an iPhone 16e at 2 threads, 5.1 s of it proving; this laptop run, 4 threads: witness 9 s, proving 15 s. |
| 7 | 1:40 | **Sent, and confirmed on mainnet:** `ba0f91cfe7ca33dd269b692bf80027df2681a321215e90ab28c2a56260fa2aad`. |
| 8 | 2:05 | **Leaving the shielded pool needs a tick.** Not the swap provider, never holding funds. |
| 9 | 2:18 | **Dry quote only.** Spread, timing, the rail's own 0.25% fee. Nothing swapped. |
| 10 | 2:35 | **Group envelopes:** 25 links, one CSV: `envelope_zec` they receive, `send_zec` you pay. |
